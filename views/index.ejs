<!DOCTYPE html>
<html>
    <head>
        <meta charset="utf-8">
        <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
        <link href="https://cdn.jsdelivr.net/npm/bootstrap@5.1.3/dist/css/bootstrap.min.css" rel="stylesheet" integrity="sha384-1BmE4kWBq78iYhFldvKuhfTAU6auU8tT94WrHftjDbrCEXSU1oBoqyl2QvZ6jIW3" crossorigin="anonymous">
        <title>connected devices</title>

        <meta http-equiv="refresh" content="2"> <!--temporarily instead react/fetch-->

    </head>

    <body>
        <h1> connected usb devices (<%= number %>) : </h1>

        <ul id="devices list">
            <% for(var i=0; i<devices.length; i++) {%>
               <li>
                    <div>
                        <strong> usb <%= i+1 %> : </strong>
                    </div>
                    <div>
                        vendor id: <%= devices[i].vendorID %>
                    </div>
                    <div>
                        product id: <%= devices[i].productID %>
                    </div>
                    <div>
                        type: <%= devices[i].type %>
                    </div>
                    <div>
                        descriptor string: <%= devices[i].stringDescriptor %>
                    </div>
                </li>
            <% } %>
        </ul>

        <!-- <script>
            document.addEventListener("DOMContentLoaded", ()=>{
                var devices = document.getElementById("devices list");
                fetch('./update').then(data=>{
                    devices.innerText = data;
                }).catch(err=>{

                });
            });
        </script> -->
    </body>
</html>